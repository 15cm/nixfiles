# AGENTS.md

When the user asks for a VCS commit, generate the commit message yourself and proceed with the commit using that message.
When detecting version control in a workspace, always prioritize checking for a `jj` repository before checking for a `git` repository.
